
function login $id

  cookie(:name, $id)
  redirect()


function logout

  cookie(:name, null)
  redirect()


function is_logged

  return !! cookie(:name)

function ticks

  return round(microtime(TRUE) - START, 4);
