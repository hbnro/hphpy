<?php

require 'haphpy/vendor/autoload.php'

if ! in_array('haphpy', stream_get_wrappers())
  stream_wrapper_register('haphpy', '\\Haphpy\\Module')


foreach explode(' ', 'ns try class traits function lambda') as $one
  require "haphpy://tests/samples/$one.haphpy"


echo $baz(Test::items()[:symbol])

echo $buzz(hello())

var_dump($foo())
